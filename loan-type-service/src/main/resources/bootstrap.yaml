spring:
  application:
    name: loan-type-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/prestabanco_microservices
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
