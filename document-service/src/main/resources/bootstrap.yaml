spring:
  application:
    name: document-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:5432/prestabancoms
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
