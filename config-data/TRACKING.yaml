server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/prestabanco-tracking
    username: postgres
    password: admin
---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:postgresql://TRACKING:5432/prestabanco-tracking
    username: postgres
    password: admin