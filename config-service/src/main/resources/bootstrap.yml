spring:
  application:
    name: config-service
  config:
    import: configserver: # Importa el Config Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nico-s-b/prestabanco-microservices
          default-label: main
          search-paths: config-data
