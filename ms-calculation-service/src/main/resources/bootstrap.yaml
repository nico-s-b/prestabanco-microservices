spring:
  application:
    name: CALCULATION
  profiles:
    active: ${PROFILE:dev}
---
spring:
  config:
    activate:
      on-profile: dev
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
---
spring:
  config:
    activate:
      on-profile: prod
  cloud:
    config:
      enabled: true
      uri: http://config-service:8081